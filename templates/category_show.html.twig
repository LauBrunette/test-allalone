{% extends 'base.html.twig' %}

    {% block metaTitle %}

        Les catégories

    {% endblock metaTitle %}

{% block main %}
    <div class="container-show">
        <h1>{{ category.title }}</h1>
        <p>{{ category.content }}</p>
    </div>
{% endblock main %}