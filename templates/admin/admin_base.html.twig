<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width", initial-scale=1.0>
        <meta http-equiv="X-UA-Compatible" content="ie-edge">
        <title>{% block metaTitle %}{% endblock metaTitle %}</title>
        <!-- la fonction asset va chercher le fichier dans le dossier public directement -->
        <!-- asset : JS, images, CSS, etc. -->
        <link rel="stylesheet" href="{{ asset('style.css') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    </head>
    <body>

        <header>

            <nav>
                <div class="nav-img">
                    <a href={{ path('home') }}><img src={{ asset('images/rose.png') }}></a>
                </div>
                <!-- Créer un formulaire avec une méthode GET pour récupérer les données (apparaît dans l'URL) -->
                <form action="{{ path('search') }}" method="get">
                    <input type="text" name="q" placeholder="Rechercher">
                    <input type="submit" value="go !">
                </form>

            </nav>
            <p>Vous êtes sur l'interface administrateur</p>


        </header>
        <main>


            <div class="flash-message">

                {% for message in app.flashes('success') %}
                    <div>
                        {{ message }}
                    </div>
                {% endfor %}

            </div>


            {% block main %}
            {% endblock main %}

        </main>
        <footer>

            <div class="footer-div">
                <ul class="footer-li">
                    <li>Mentions mégales</li>
                    <li>Contact</li>
                    <li>A propos</li>
                </ul>
            </div>

        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    </body>
</html>
